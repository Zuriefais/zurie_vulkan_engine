[package]
name = "zurie_types"
version = "0.0.0"
edition = "2024"

[dependencies]
glam = { version = "0.30.1", features = ["mint", "serde"] }
num_enum = "*"
serde = { workspace = true }
slotmap = {workspace = true}
